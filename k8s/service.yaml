apiVersion: v1
kind: Service
metadata:
  name: cinesearch-service
spec:
  type: NodePort                       # Required for Docker Desktop + other PC access
  selector:
    app: cinesearch                    # Must match deployment labels
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007                    # Valid range: 30000–32767